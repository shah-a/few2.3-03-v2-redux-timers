import formatTime from './formatTime';
import { saveState, loadState } from './dataPersistence';

// eslint-disable-next-line import/prefer-default-export
export { formatTime, saveState, loadState };
